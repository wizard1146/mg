mg = typeof mg != 'undefined' ? mg : {}

mg.constructs = (function() {
  /* Meta variables */
  
  /* Module Settings & Events */
  
  /* Memory */
  
  /* Computational variables */

  

  

  return {
  
  }
})()